# ==================================
# SSO Configuration Example
# ==================================

# Session Configuration
SSO_SESSION_SECRET=your-secret-key-min-32-characters-long-change-this-in-production
SSO_SESSION_MAX_AGE=3600000
SSO_SESSION_COOKIE_NAME=sso_session
SSO_SESSION_SECURE=true
SSO_CSRF_PROTECTION=true
SSO_STATE_EXPIRATION=600000
SSO_ALLOWED_ORIGINS=https://yourapp.com,https://www.yourapp.com

# ==================================
# Google Workspace Configuration
# ==================================
SSO_GOOGLE_ENABLED=true
SSO_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
SSO_GOOGLE_CLIENT_SECRET=your-google-client-secret
SSO_GOOGLE_CALLBACK_URL=https://yourapp.com/auth/sso/callback/google-workspace
SSO_GOOGLE_DOMAIN=yourcompany.com
SSO_GOOGLE_HOSTED_DOMAIN=yourcompany.com
SSO_GOOGLE_SCOPES=openid,email,profile

# ==================================
# Okta Configuration (OIDC)
# ==================================
SSO_OKTA_ENABLED=true
SSO_OKTA_PROTOCOL=oidc
SSO_OKTA_DOMAIN=dev-123456.okta.com
SSO_OKTA_CLIENT_ID=your-okta-client-id
SSO_OKTA_CLIENT_SECRET=your-okta-client-secret
SSO_OKTA_CALLBACK_URL=https://yourapp.com/auth/sso/callback/okta
SSO_OKTA_SCOPES=openid,email,profile,groups

# Okta SAML Configuration (Alternative)
# SSO_OKTA_PROTOCOL=saml
# SSO_OKTA_ISSUER=http://www.okta.com/exk1234567890
# SSO_OKTA_ENTRY_POINT=https://dev-123456.okta.com/app/dev-123456_yourapp_1/exk1234567890/sso/saml
# SSO_OKTA_CERT=MIIDpDCCAoygAwIBAgIGAXoWn...

# ==================================
# Azure AD Configuration (OIDC)
# ==================================
SSO_AZURE_ENABLED=true
SSO_AZURE_PROTOCOL=oidc
SSO_AZURE_TENANT_ID=your-tenant-id-or-common
SSO_AZURE_CLIENT_ID=your-azure-client-id
SSO_AZURE_CLIENT_SECRET=your-azure-client-secret
SSO_AZURE_CALLBACK_URL=https://yourapp.com/auth/sso/callback/azure-ad
SSO_AZURE_SCOPES=openid,profile,email,User.Read
SSO_AZURE_DOMAIN_HINT=yourcompany.com
# SSO_AZURE_LOGIN_HINT=user@yourcompany.com
# SSO_AZURE_PROMPT=select_account

# Azure AD SAML Configuration (Alternative)
# SSO_AZURE_PROTOCOL=saml
# SSO_AZURE_ISSUER=spn:your-app-id
# SSO_AZURE_ENTRY_POINT=https://login.microsoftonline.com/your-tenant-id/saml2
# SSO_AZURE_CERT=MIIDpDCCAoygAwIBAgIGAXoWn...

# ==================================
# Provider-Specific Notes
# ==================================

# Google Workspace:
# 1. Create OAuth 2.0 credentials in Google Cloud Console
# 2. Add authorized redirect URIs
# 3. Enable Google+ API (for userinfo)
# 4. Optional: Restrict to your G Suite domain with HOSTED_DOMAIN

# Okta:
# 1. Create an OIDC or SAML application in Okta Admin Console
# 2. For OIDC: Use "Web" application type
# 3. For SAML: Download IdP metadata and extract cert
# 4. Add redirect URIs and configure attribute mappings

# Azure AD:
# 1. Register application in Azure Portal
# 2. Add redirect URIs under "Authentication"
# 3. Create client secret under "Certificates & secrets"
# 4. Add API permissions: User.Read, profile, email, openid
# 5. For SAML: Use "Enterprise Application" and download certificate

# ==================================
# Security Best Practices
# ==================================
# 1. Use HTTPS in production (SSO_SESSION_SECURE=true)
# 2. Generate strong session secret (min 32 characters)
# 3. Enable CSRF protection
# 4. Restrict ALLOWED_ORIGINS to your actual domains
# 5. Use environment-specific configuration files
# 6. Never commit secrets to version control
# 7. Rotate secrets regularly
# 8. Use separate credentials for dev/staging/production
