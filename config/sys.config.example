%% CRE sys.config - copy to sys.config and customize.
%% Do NOT commit sys.config if it contains your API key.
[
 {cre, [
   %% Z.AI API key (or use ZAI_API_KEY env)
   {zai_api_key, "YOUR_ZAI_API_KEY"},
   %% Model (or use ZAI_MODEL env; default: glm-4.7-flash)
   {zai_model, "glm-4.7-flash"}
 ]}
].
