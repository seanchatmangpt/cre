%%% @doc OTP application module for {{project.name}}
%%% Generated by ggen
-module({{project.name}}_app).

-behaviour(application).

-export([start/2, stop/1]).

start(_StartType, _StartArgs) ->
    {{project.name}}_sup:start_link().

stop(_State) ->
    ok.
