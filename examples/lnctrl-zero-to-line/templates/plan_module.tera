%%% @doc Generated plan module for {{project.name}}
%%% DO NOT EDIT - Generated by ggen from ontology/order_line.ttl
%%% Generation timestamp: {{timestamp}}
-module({{project.name}}_plan).

-export([plan/0]).

%% @doc Return the ln_ctrl plan term for this line
-spec plan() -> ln_ctrl:plan().
plan() ->
    %% Root plan generated from RDF ontology
    {seq, [
        {task, validate_order},
        {scope, fulfillment_scope, {seq, [
            {par, [
                {task, reserve_inventory},
                {task, check_stock}
            ]},
            {scope, payment_scope, {task, process_payment}},
            {task, ship_order}
        ]}},
        {task, notify_customer}
    ]}.

%%%
%%% NOTE: This is a simplified manual generation for MVP.
%%% Full implementation would parse plan_nodes query results
%%% and recursively build the plan term structure.
%%%
%%% Plan primitives supported:
%%% - {task, TaskId}
%%% - {seq, [Plan, ...]}
%%% - {par, [Plan, ...]}
%%% - {xor, [Plan, ...]}
%%% - {scope, ScopeId, BodyPlan}
%%% - {join, all}
%%%
