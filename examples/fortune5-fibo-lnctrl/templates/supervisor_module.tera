%% Generated supervisor module for {{ appId }}
%% DO NOT EDIT - Generated by Fortune-5 FIBO LineController Factory

-module({{ appId }}_sup).
-behaviour(supervisor).

%% API
-export([start_link/0]).

%% Supervisor callbacks
-export([init/1]).

-define(SERVER, ?MODULE).

%%% API

%% @doc Start the supervisor
-spec start_link() -> {ok, pid()} | {error, term()}.
start_link() ->
    supervisor:start_link({local, ?SERVER}, ?MODULE, []).

%%% Supervisor callbacks

%% @doc Initialize the supervisor
-spec init(Args :: term()) -> {ok, {supervisor:sup_flags(), [supervisor:child_spec()]}}.
init([]) ->
    SupFlags = #{
        strategy => {{ strategy | default(value="one_for_one") }},
        intensity => {{ intensity | default(value="10") }},
        period => {{ period | default(value="60") }}
    },

    %% Define child worker specifications
    ChildSpecs = [
        #{
            id => {{ appId }}_worker_1,
            start => { {{ appId }}_worker, start_link, [#{worker_id => 1}]},
            restart => permanent,
            shutdown => 5000,
            type => worker,
            modules => [{{ appId }}_worker]
        },
        #{
            id => {{ appId }}_worker_2,
            start => { {{ appId }}_worker, start_link, [#{worker_id => 2}]},
            restart => permanent,
            shutdown => 5000,
            type => worker,
            modules => [{{ appId }}_worker]
        }
    ],

    {ok, {SupFlags, ChildSpecs}}.

%%% EUnit Tests

-ifdef(TEST).
-include_lib("eunit/include/eunit.hrl").

start_link_test() ->
    {ok, Pid} = start_link(),
    ?assert(is_pid(Pid)),
    exit(Pid, shutdown),
    timer:sleep(100).

-endif.
