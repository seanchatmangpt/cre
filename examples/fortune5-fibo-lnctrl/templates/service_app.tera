%% Generated service application module for {{ connectorId }}
%% DO NOT EDIT - Generated by Fortune-5 FIBO LineController Factory

-module(f5_service_{{ connectorId }}_app).
-behaviour(application).

%% Application callbacks
-export([start/2, stop/1]).

%%% API

%% @doc Start the service application
-spec start(StartType :: application:start_type(), StartArgs :: term()) ->
    {ok, pid()} | {error, term()}.
start(_StartType, _StartArgs) ->
    f5_service_{{ connectorId }}_sup:start_link().

%% @doc Stop the service application
-spec stop(State :: term()) -> ok.
stop(_State) ->
    ok.

%%% EUnit Tests

-ifdef(TEST).
-include_lib("eunit/include/eunit.hrl").

start_stop_test() ->
    {ok, Pid} = start(normal, []),
    ?assert(is_pid(Pid)),
    ?assertEqual(ok, stop([])).

-endif.
